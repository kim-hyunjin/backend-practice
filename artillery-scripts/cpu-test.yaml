config:
  target: "{URL}"
  phases:
    - duration: 60 # 테스트 수행 시간
      arrivalRate: 1 # 초당 api 요청 횟수
      name: Warm up
scenarios:
  # We define one scenario:
  - name: "just get hash"
    flow:
      - get:
          url: "/hash/123"
